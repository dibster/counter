'use strict'
function add(a,b) {
    return a+ b;
}

function multiply (a,b) {
    return a * b;
}

let flock_a = 4
let flock_b = 2
let flock_c = 0

let result = add(multiply(add(flock_a,flock_c),flock_b),multiply(flock_a,flock_b));

let r2 = multiply(flock_b, add(flock_a, flock_a));

console.log(result);
console.log(r2);
